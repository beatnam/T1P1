<?xml version="1.0" encoding="UTF-8"?>

<!--
	https://mybatis.org/mybatis-3/getting-started.html 
	src/main/resources/mapper폴더/MemberMapper.xml파일 
	
-->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.CorporationMapper">

  <insert id="insertMember">
    insert into corporation(coporationmember_id, corporationmember_name, corporationmember_passwd,corporationmember_email,
    corporation_name, corporation_location, corporationmember_phone,corporation_url,corporation_photo,corporation_registration,
    corporation_registrationpdf,corporation_intro, corporation_verified)
        values (#{coporationMemberId},#{corporationMemberName},#{corporationMemberPasswd},#{corporationMemberEmail},
        #{corporationName},#{corporationLocation},#{corporationMemberPhone},#{corporationUrl},#{corporationPhoto}, #{corporationRegistration},
        #{corporationRegistrationPdf},#{corporationIntro}, #{corporationVerified} )
  </insert>
  
  <select id="loginMember" resultType="com.itwillbs.domain.MemberDTO">
  	select * from members
  	where id = #{id} and passwd = #{passwd}
  </select>
  
  <select id="infoMember" resultType="com.itwillbs.domain.MemberDTO">
  	select * from members
  	where id = #{id}
  </select>
  
  <update id="updateMember">
  	update members
  	set name = #{name}, phone = #{phone}, email = #{email}, gender = #{gender}, agree = #{agree}
  	where id = #{id} 
  </update>
  
  <delete id="deleteMember">
  	delete from members
  	where id = #{id} and passwd = #{passwd}
  </delete>
  
  <select id="listMember" resultType="com.itwillbs.domain.MemberDTO">
  	select * from members
  </select>
  
</mapper>